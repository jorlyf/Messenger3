import * as React from "react";
import CreateGroupDialogModal from "./CreateGroupDialogModal";

const Modals: React.FC = () => {
  return (
    <CreateGroupDialogModal />
  );
}

export default Modals;